#include <stdio.h>
int main()
{
    int a[5] = {1, 2, 3};
    // a[2]=10;
    // scanf("%d", &a[10]);
    // int sum = 0;
    // for (int i = 0; i < 3; i++)
    // {
    //     sum += a[i];
    // }
    printf("%d", a[4]);

    return 0;
}